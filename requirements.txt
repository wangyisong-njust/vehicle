# Versions below are the exact stack used to generate the reported results.
# The short-time deep models train on CPU over 40 epochs; their numbers are
# sensitive to the PyTorch / BLAS version, so a different torch build can shift
# Macro-F1 by a few points on the 94-sample test set and even change the
# ablation ordering. Use these versions to reproduce the report numbers.
numpy==1.26.4
pandas==2.3.2
scipy==1.15.3
scikit-learn==1.7.1
xgboost==2.1.4
torch==2.9.0
opencv-python>=4.8
matplotlib>=3.7
tqdm>=4.66
eclipse-sumo==1.22.0
